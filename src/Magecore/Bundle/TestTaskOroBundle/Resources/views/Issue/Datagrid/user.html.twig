{% if resource_granted('testtaskoro.issue_view') %}
    {% import 'OroDataGridBundle::macros.html.twig' as dataGrid %}

    <div class="responsive-cell">
        <div class="box-type1">
            <div class="title">
                <span class="widget-title">{{ "magecore.testtaskoro.issue.entity_plural_label"|trans }}</span>
            </div>

            <div class="row-fluid">
                {{ dataGrid.renderGrid('issue-assigned-grid', {id: entity.id}) }}
            </div>
        </div>
    </div>
{% endif %}
