{% set ownerId = record.getValue('ownerId') %}
{% if resource_granted('oro_user_user_view') and ownerId %}
    <a href="{{ path('oro_user_view', {id: ownerId}) }}">
        {{ value }}
    </a>
{% else %}
    {{ value }}
{% endif %}
